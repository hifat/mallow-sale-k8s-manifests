apiVersion: v1
kind: Service
metadata:
  name: mallow-sale-api-service
  namespace: mallow-sale
spec:
  selector:
    app: mallow-sale-api
  ports:
    - name: http
      protocol: TCP
      port: 4000
      targetPort: 4000
  type: ClusterIP